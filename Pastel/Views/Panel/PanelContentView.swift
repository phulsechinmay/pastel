import SwiftUI

/// Root SwiftUI view hosted inside the sliding panel.
/// Placeholder for Task 1 compilation -- Task 2 replaces with full implementation.
struct PanelContentView: View {
    var body: some View {
        Text("Panel Content")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
