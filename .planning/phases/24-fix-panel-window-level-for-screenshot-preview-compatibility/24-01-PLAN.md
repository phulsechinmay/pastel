---
phase: 24-fix-panel-window-level-for-screenshot-preview-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pastel/Views/Panel/SlidingPanel.swift
  - Pastel/Services/ColorToolController.swift
autonomous: false

must_haves:
  truths:
    - "macOS screenshot preview thumbnail appears in front of the Pastel panel"
    - "Panel still appears above the Dock when open"
    - "Liquid Glass rendering on the panel is not degraded"
  artifacts:
    - path: "Pastel/Views/Panel/SlidingPanel.swift"
      provides: "Panel window level at 23 (above Dock, below Screenshot)"
      contains: "CGWindowLevelForKey"
    - path: "Pastel/Services/ColorToolController.swift"
      provides: "Color panel window level matching the sliding panel"
      contains: "CGWindowLevelForKey"
  key_links:
    - from: "Pastel/Views/Panel/SlidingPanel.swift"
      to: "CGWindowLevelForKey(.dockWindow)"
      via: "Custom NSWindow.Level derived from dock level + 3"
      pattern: "CGWindowLevelForKey\\(.dockWindow\\)"
    - from: "Pastel/Services/ColorToolController.swift"
      to: "CGWindowLevelForKey(.dockWindow)"
      via: "Same custom level as SlidingPanel"
      pattern: "CGWindowLevelForKey\\(.dockWindow\\)"
---

<objective>
Lower the Pastel panel window level from `.statusBar` (25) to a custom level 23 so that macOS screenshot preview thumbnails (rendered at level 24) appear above the panel, while the panel remains above the Dock (level 20).

Purpose: Screenshot previews are currently hidden behind the panel, making it impossible to interact with them while the panel is open.
Output: Updated SlidingPanel and ColorToolController with consistent window level 23.
</objective>

<execution_context>
@/Users/phulsechinmay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phulsechinmay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Pastel/Views/Panel/SlidingPanel.swift
@Pastel/Services/ColorToolController.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change panel and color picker window levels from .statusBar to custom level 23</name>
  <files>Pastel/Views/Panel/SlidingPanel.swift, Pastel/Services/ColorToolController.swift</files>
  <action>
In `SlidingPanel.swift`, replace `level = .statusBar` (line 21) with:
```swift
// Above Dock (20) and Notification Center (21), below Screenshot overlay (24)
let dockLevel = Int(CGWindowLevelForKey(.dockWindow))
level = NSWindow.Level(rawValue: dockLevel + 3)  // 23
```
Add `import CoreGraphics` at the top if not already present (AppKit re-exports it, but explicit import is cleaner for CGWindowLevelForKey usage). Check first -- if AppKit already provides it, skip the extra import.

In `ColorToolController.swift`, replace `panel.level = .statusBar` (line 26) with:
```swift
let dockLevel = Int(CGWindowLevelForKey(.dockWindow))
panel.level = NSWindow.Level(rawValue: dockLevel + 3)  // 23 -- match SlidingPanel
```

Update the comment on line 19 of SlidingPanel.swift: change "above regular windows and the dock" to "above Dock (20), below Screenshot overlay (24)".

Do NOT change any other properties (isFloatingPanel, collectionBehavior, etc.).
  </action>
  <verify>
Build the project with `xcodebuild -project Pastel.xcodeproj -scheme Pastel -configuration Debug build`. Confirm zero errors related to window level changes.
  </verify>
  <done>Both SlidingPanel and ColorToolController use `CGWindowLevelForKey(.dockWindow) + 3` (level 23) instead of `.statusBar` (level 25). Project builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify panel level behavior -- above Dock, below screenshot preview</name>
  <action>Changed the panel window level from 25 (statusBar) to 23 (dockWindow + 3) in both SlidingPanel and ColorToolController. This should place the panel above the Dock but below the macOS screenshot floating thumbnail.</action>
  <verify>
1. Launch the app (Cmd+R in Xcode or run the built binary)
2. Open the Pastel panel (Cmd+Shift+V or click the menu bar icon)
3. Verify the panel appears ABOVE the Dock (panel should overlap the Dock area, not hide behind it)
4. With the panel still open, take a screenshot (Cmd+Shift+4, select any area, or Cmd+Shift+3 for full screen)
5. Watch for the screenshot floating thumbnail in the bottom-right corner -- it should appear IN FRONT of the Pastel panel, not behind it
6. Click the floating thumbnail to verify it is interactive (opens screenshot markup/preview)
7. Open the color picker tool from the panel toolbar, verify it also appears correctly relative to the panel
8. Verify Liquid Glass rendering looks normal on the panel (no degraded/flat glass effects)
  </verify>
  <done>Panel appears above Dock, screenshot thumbnail appears above panel and is interactive, color picker level is consistent, Liquid Glass renders correctly.</done>
</task>

</tasks>

<verification>
- [ ] Panel renders above the Dock when open
- [ ] macOS screenshot floating thumbnail appears above the panel
- [ ] Screenshot thumbnail is clickable/interactive with panel open
- [ ] Color picker panel level is consistent with main panel
- [ ] Liquid Glass rendering is not degraded
- [ ] Project builds without errors
</verification>

<success_criteria>
The macOS screenshot preview thumbnail is visible and interactive when the Pastel panel is open, while the panel continues to float above the Dock. Both SlidingPanel and NSColorPanel use the same custom window level derived from `CGWindowLevelForKey(.dockWindow) + 3`.
</success_criteria>

<output>
After completion, create `.planning/phases/24-fix-panel-window-level-for-screenshot-preview-compatibility/24-01-SUMMARY.md`
</output>
