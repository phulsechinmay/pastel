---
phase: 19-cloudkit-compatible-data-model
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - Pastel/Services/ClipboardMonitor.swift
  - Pastel/Services/MigrationService.swift
  - Pastel/Services/ImportExportService.swift
  - Pastel/App/AppState.swift
  - Pastel/Views/Panel/ClipboardCardView.swift
  - Pastel/Views/Panel/FilteredCardListView.swift
  - Pastel/Views/Panel/EditItemView.swift
  - Pastel/Views/Settings/HistoryGridView.swift
  - Pastel/Views/Settings/HistoryBrowserView.swift
autonomous: true

must_haves:
  truths:
    - "User copies the same text twice non-consecutively and only one item appears in history (application-level hash dedup)"
    - "User copies any item and it has an originDeviceID stamped (verifiable via export)"
    - "All .labels access throughout the codebase uses .safeLabels (no compile errors, no nil crashes)"
    - "App runs identically to v1.4 from user perspective -- no behavioral changes"
    - "Project builds and runs without errors or warnings related to optional array access"
  artifacts:
    - path: "Pastel/Services/ClipboardMonitor.swift"
      provides: "Application-level hash dedup + originDeviceID stamping"
      contains: "isDuplicateByHash"
    - path: "Pastel/Views/Panel/ClipboardCardView.swift"
      provides: "Nil-safe label access in card views"
      contains: "safeLabels"
    - path: "Pastel/Views/Panel/FilteredCardListView.swift"
      provides: "Nil-safe label filtering and assignment"
      contains: "safeLabels"
    - path: "Pastel/Services/ImportExportService.swift"
      provides: "Nil-safe export and import label wiring"
      contains: "safeLabels"
  key_links:
    - from: "Pastel/Services/ClipboardMonitor.swift"
      to: "ClipboardItem.contentHash"
      via: "fetchCount predicate check before insert"
      pattern: "isDuplicateByHash"
    - from: "Pastel/Services/ClipboardMonitor.swift"
      to: "DeviceIdentifier.current"
      via: "originDeviceID assignment on new items"
      pattern: "originDeviceID.*DeviceIdentifier"
    - from: "Pastel/Views/Panel/ClipboardCardView.swift"
      to: "ClipboardItem.safeLabels"
      via: "computed property nil-coalescing"
      pattern: "\\.safeLabels"
    - from: "Pastel/Services/ImportExportService.swift"
      to: "ClipboardItem.safeLabels"
      via: "export mapping and import wiring"
      pattern: "safeLabels\\.map|safeLabels\\.append"
---

<objective>
Update all call sites to use nil-safe relationship accessors, add application-level hash deduplication to replace the removed @Attribute(.unique) constraint, and stamp originDeviceID on newly captured items.

Purpose: With the model changes from Plan 01, the codebase has compile errors at every `.labels` access site (now optional). This plan fixes all 22+ call sites, adds the critical dedup logic that prevents duplicate items without the unique constraint, and wires originDeviceID stamping into the capture flow.

Output: Fully compiling, running application with CloudKit-compatible behavior
</objective>

<execution_context>
@/Users/phulsechinmay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phulsechinmay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cloudkit-compatible-data-model/19-RESEARCH.md
@.planning/phases/19-cloudkit-compatible-data-model/19-01-SUMMARY.md
@Pastel/Services/ClipboardMonitor.swift
@Pastel/Services/MigrationService.swift
@Pastel/Services/ImportExportService.swift
@Pastel/App/AppState.swift
@Pastel/Views/Panel/ClipboardCardView.swift
@Pastel/Views/Panel/FilteredCardListView.swift
@Pastel/Views/Panel/EditItemView.swift
@Pastel/Views/Settings/HistoryGridView.swift
@Pastel/Views/Settings/HistoryBrowserView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add application-level hash dedup and originDeviceID stamping to ClipboardMonitor</name>
  <files>Pastel/Services/ClipboardMonitor.swift</files>
  <action>
  Add a new private method `isDuplicateByHash(_:)` to ClipboardMonitor that checks for ANY existing item with the same contentHash using `fetchCount`:

  ```swift
  /// Check if ANY item in history has the same content hash (non-consecutive dedup).
  /// Replaces @Attribute(.unique) constraint behavior removed for CloudKit compatibility.
  private func isDuplicateByHash(_ contentHash: String) -> Bool {
      let hashToCheck = contentHash
      let descriptor = FetchDescriptor<ClipboardItem>(
          predicate: #Predicate<ClipboardItem> { $0.contentHash == hashToCheck }
      )
      do {
          let count = try modelContext.fetchCount(descriptor)
          if count > 0 {
              Self.logger.debug("Non-consecutive duplicate detected by hash, skipping")
              return true
          }
      } catch {
          Self.logger.error("Hash dedup check failed: \(error.localizedDescription)")
          // Continue with insertion on error -- better to have a dup than lose data
      }
      return false
  }
  ```

  In `processPasteboardContent()`:
  - After the existing `isDuplicateOfMostRecent(contentHash:)` call (line 247), add: `if isDuplicateByHash(contentHash) { return }`
  - After `modelContext.insert(item)` (line 270), add: `item.originDeviceID = DeviceIdentifier.current`
  - The save catch block (lines 308-313) currently handles unique constraint violations. Update the comment to reflect that this block now handles general save errors only (unique constraint no longer exists). Keep the rollback behavior.

  In `processImageContent(isConcealed:)`:
  - After `isDuplicateOfMostRecent(contentHash:)` (line 334), add: `if isDuplicateByHash(contentHash) { return }`
  - After `self.modelContext.insert(item)` (line 365), add: `item.originDeviceID = DeviceIdentifier.current`
  - Update the save catch block comment similarly.

  Keep `isDuplicateOfMostRecent()` as-is -- it provides a fast O(1) short-circuit for the common case of rapid repeated Cmd+C. `isDuplicateByHash()` catches the uncommon case of same content copied hours apart.

  Add `import Foundation` if not already present (it is, via AppKit).
  </action>
  <verify>Build the project: `xcodebuild build -scheme Pastel -destination 'platform=macOS' 2>&1 | grep -c 'error:'` should show only errors from .labels call sites in view files (not from ClipboardMonitor). Verify ClipboardMonitor.swift has zero errors by checking it compiles in isolation context.</verify>
  <done>ClipboardMonitor has isDuplicateByHash() called before every insert (both text and image paths), originDeviceID is stamped via DeviceIdentifier.current on every new item, and save catch blocks are updated for post-unique-constraint behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update all .labels and .items call sites to use safeLabels/safeItems</name>
  <files>
    Pastel/Views/Panel/ClipboardCardView.swift,
    Pastel/Views/Panel/FilteredCardListView.swift,
    Pastel/Views/Panel/EditItemView.swift,
    Pastel/Views/Settings/HistoryGridView.swift,
    Pastel/Views/Settings/HistoryBrowserView.swift,
    Pastel/Services/ImportExportService.swift,
    Pastel/Services/MigrationService.swift,
    Pastel/App/AppState.swift
  </files>
  <action>
  Perform a mechanical find-and-replace of `item.labels.` to `item.safeLabels.` across all call sites listed below. Do NOT change model init assignments (`self.labels = []` in ClipboardItem.swift, `self.items = []` in Label.swift) or the `@Relationship` declaration -- those must remain on the backing property.

  **ClipboardCardView.swift (8 sites):**
  - `item.labels.prefix(3)` -> `item.safeLabels.prefix(3)`
  - `item.labels.count > 3` -> `item.safeLabels.count > 3`
  - `item.labels.count - 3` -> `item.safeLabels.count - 3`
  - `item.labels.contains` -> `item.safeLabels.contains`
  - `item.labels.removeAll` (2 occurrences) -> `item.safeLabels.removeAll`
  - `item.labels.append` -> `item.safeLabels.append`
  - `item.labels.isEmpty` -> `item.safeLabels.isEmpty`

  **FilteredCardListView.swift (3 sites):**
  - `item.labels.contains` (2 occurrences) -> `item.safeLabels.contains`
  - `item.labels.append(label)` -> `item.safeLabels.append(label)`

  **EditItemView.swift (3 sites):**
  - `item.labels.contains` -> `item.safeLabels.contains`
  - `item.labels.removeAll` -> `item.safeLabels.removeAll`
  - `item.labels.append` -> `item.safeLabels.append`

  **HistoryGridView.swift (1 site):**
  - `item.labels.contains` -> `item.safeLabels.contains`

  **HistoryBrowserView.swift (1 site):**
  - `item.labels.removeAll()` -> `item.safeLabels.removeAll()`

  **ImportExportService.swift (2 sites):**
  - Line 122: `item.labels.map(\.name)` -> `item.safeLabels.map(\.name)`
  - Line 252: `item.labels.append(label)` -> `item.safeLabels.append(label)`

  **MigrationService.swift (2 sites):**
  - `item.labels.contains(where:)` -> `item.safeLabels.contains(where:)`
  - `item.labels.append(singleLabel)` -> `item.safeLabels.append(singleLabel)`

  **AppState.swift (1 site):**
  - `item.labels.removeAll()` -> `item.safeLabels.removeAll()`

  After making all changes, do a project-wide grep for `item\.labels\.` (excluding model files and comments) to verify no call sites were missed. The only remaining `.labels` references should be:
  - ClipboardItem.swift: the `var labels: [Label]?` declaration, the `safeLabels` computed property, and `self.labels = []` in init
  - Label.swift: comments referencing "item.labels"
  - SettingsView.swift: `.labels` is a SettingsTab enum case (unrelated)
  - ImportExportService.swift: `export.labels` is on PastelExport struct (unrelated)
  </action>
  <verify>
  1. Build: `xcodebuild build -scheme Pastel -destination 'platform=macOS' 2>&1 | tail -5` -- expect BUILD SUCCEEDED with zero errors
  2. Grep verification: `grep -rn 'item\.labels\.' Pastel/ --include='*.swift' | grep -v 'safeLabels' | grep -v 'Models/' | grep -v '//' | grep -v 'export\.'` should return zero lines (no missed call sites)
  </verify>
  <done>All 22 call sites across 8 files updated from .labels to .safeLabels. Project compiles with zero errors. No .labels access remains outside model declarations.</done>
</task>

</tasks>

<verification>
1. `xcodebuild build -scheme Pastel -destination 'platform=macOS'` succeeds with zero errors
2. Grep for `@Attribute(.unique)` returns zero matches
3. Grep for `item\.labels\.` outside model files returns zero matches (all use .safeLabels)
4. Grep for `isDuplicateByHash` shows calls in both processPasteboardContent and processImageContent
5. Grep for `originDeviceID` shows assignment in both text and image capture paths
6. Grep for `DeviceIdentifier.current` shows usage in ClipboardMonitor
7. App launches without crash on existing v1.4 database (auto-migration succeeds)
</verification>

<success_criteria>
The app builds, runs, and behaves identically to v1.4 from the user's perspective. Non-consecutive duplicates are caught by application-level hash check. New items are stamped with originDeviceID. All relationship access is nil-safe. The schema is fully CloudKit-compatible and ready for Phase 20 to enable sync.
</success_criteria>

<output>
After completion, create `.planning/phases/19-cloudkit-compatible-data-model/19-02-SUMMARY.md`
</output>
