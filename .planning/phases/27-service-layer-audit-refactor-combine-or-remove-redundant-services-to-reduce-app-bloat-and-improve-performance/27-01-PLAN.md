---
phase: 27-service-layer-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Pastel/Services/SwiftDataHelpers.swift
  - Pastel/App/AppState.swift
  - Pastel/Views/Panel/ClipboardCardView.swift
  - Pastel/Views/Settings/HistoryBrowserView.swift
  - Pastel/Services/ExpirationService.swift
  - Pastel/Services/RetentionService.swift
  - Pastel/Services/ClipboardMonitor.swift
  - Pastel/Services/ImportExportService.swift
  - Pastel/Services/ImageStorageService.swift
autonomous: true

must_haves:
  truths:
    - "Deleting a clipboard item from any location (card, bulk delete, clear all, retention purge, concealed expiration) cleans up disk images, URL metadata images, and label relationships identically"
    - "SHA256 hashing for text and image content uses a single source of truth"
    - "App builds and runs with no regressions after refactoring"
  artifacts:
    - path: "Pastel/Services/SwiftDataHelpers.swift"
      provides: "deleteClipboardItemWithCleanup() free function and ContentHash enum"
      contains: "deleteClipboardItemWithCleanup"
    - path: "Pastel/Services/SwiftDataHelpers.swift"
      provides: "ContentHash utility enum"
      contains: "enum ContentHash"
  key_links:
    - from: "Pastel/App/AppState.swift"
      to: "SwiftDataHelpers.swift"
      via: "deleteClipboardItemWithCleanup call"
      pattern: "deleteClipboardItemWithCleanup"
    - from: "Pastel/Views/Panel/ClipboardCardView.swift"
      to: "SwiftDataHelpers.swift"
      via: "deleteClipboardItemWithCleanup call"
      pattern: "deleteClipboardItemWithCleanup"
    - from: "Pastel/Views/Settings/HistoryBrowserView.swift"
      to: "SwiftDataHelpers.swift"
      via: "deleteClipboardItemWithCleanup call"
      pattern: "deleteClipboardItemWithCleanup"
    - from: "Pastel/Services/ClipboardMonitor.swift"
      to: "SwiftDataHelpers.swift"
      via: "ContentHash.hash call"
      pattern: "ContentHash\\.hash"
    - from: "Pastel/Services/ImportExportService.swift"
      to: "SwiftDataHelpers.swift"
      via: "ContentHash.hash call"
      pattern: "ContentHash\\.hash"
    - from: "Pastel/Services/ImageStorageService.swift"
      to: "SwiftDataHelpers.swift"
      via: "ContentHash.hash call"
      pattern: "ContentHash\\.hash"
---

<objective>
Extract duplicated deletion and hashing logic into centralized helpers.

Purpose: Item deletion with image cleanup is duplicated across 5 call sites, and SHA256 hashing across 3 call sites. Centralizing these eliminates duplication and ensures consistent behavior (especially safeLabels.removeAll() for CloudKit compatibility, which 2 of 5 deletion sites currently miss).

Output: Updated SwiftDataHelpers.swift with deleteClipboardItemWithCleanup() and ContentHash enum; all call sites refactored to use them.
</objective>

<execution_context>
@/Users/phulsechinmay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phulsechinmay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-service-layer-audit-refactor-combine-or-remove-redundant-services-to-reduce-app-bloat-and-improve-performance/27-RESEARCH.md
@Pastel/Services/SwiftDataHelpers.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract deleteClipboardItemWithCleanup and ContentHash into SwiftDataHelpers.swift</name>
  <files>Pastel/Services/SwiftDataHelpers.swift</files>
  <action>
Add two new items to the existing SwiftDataHelpers.swift file (which currently contains only saveWithLogging):

1. Add `import CryptoKit` at the top.

2. Add `ContentHash` enum with two static methods:
   - `hash(text: String) -> String` — creates `Data(text.utf8)`, runs `SHA256.hash(data:)`, returns lowercase hex string via `compactMap { String(format: "%02x", $0) }.joined()`.
   - `hash(imageData: Data) -> String` — takes `imageData.prefix(4096)` (first 4KB), runs SHA256, returns same hex format.

3. Add `deleteClipboardItemWithCleanup(_ item: ClipboardItem, from modelContext: ModelContext)` as a @MainActor free function (matching the existing saveWithLogging pattern — free function, not extension, per decision [18-01]):
   - Call `ImageStorageService.shared.deleteImage(imagePath: item.imagePath, thumbnailPath: item.thumbnailPath)` for clipboard images.
   - Call `ImageStorageService.shared.deleteImage(imagePath: item.urlFaviconPath, thumbnailPath: item.urlPreviewImagePath)` for URL metadata images.
   - Call `item.safeLabels.removeAll()` for CloudKit relationship cleanup (CRITICAL — currently missing from ClipboardCardView.deleteItem and ExpirationService.performExpiration/expireOverdueItems).
   - Call `modelContext.delete(item)`.
   - Do NOT call save — callers handle their own save strategy (some batch multiple deletes before saving, some use saveWithLogging).
  </action>
  <verify>Build succeeds: `cd /Users/phulsechinmay/Desktop/Projects/pastel && xcodebuild -project Pastel.xcodeproj -scheme "Pastel Sparkle" -configuration Debug build 2>&1 | tail -5`</verify>
  <done>SwiftDataHelpers.swift contains both ContentHash enum and deleteClipboardItemWithCleanup function, builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 5 deletion call sites and 3 hash call sites with centralized helpers</name>
  <files>
    Pastel/App/AppState.swift
    Pastel/Views/Panel/ClipboardCardView.swift
    Pastel/Views/Settings/HistoryBrowserView.swift
    Pastel/Services/ExpirationService.swift
    Pastel/Services/RetentionService.swift
    Pastel/Services/ClipboardMonitor.swift
    Pastel/Services/ImportExportService.swift
    Pastel/Services/ImageStorageService.swift
  </files>
  <action>
**Deletion call sites (replace manual pattern with deleteClipboardItemWithCleanup):**

1. **ClipboardCardView.swift** — In `deleteItem()` (around line 273-284): Replace the two `ImageStorageService.shared.deleteImage` calls + `modelContext.delete(item)` with `deleteClipboardItemWithCleanup(item, from: modelContext)`. Keep the `saveWithLogging` and `appState.itemCount -= 1` lines that follow. This also FIXES the missing `safeLabels.removeAll()` that was absent here.

2. **HistoryBrowserView.swift** — In `bulkDelete()` (around line 160-174): Replace the inner loop body (two deleteImage calls + safeLabels.removeAll + modelContext.delete) with just `deleteClipboardItemWithCleanup(item, from: modelContext)`. Keep the `saveWithLogging`, `appState.itemCount -= ...`, and `selectedIDs.removeAll()` that follow the loop.

3. **AppState.swift** — In `clearAllHistory()` (around line 173-184): Replace the inner loop body (two deleteImage calls + safeLabels.removeAll + modelContext.delete) with `deleteClipboardItemWithCleanup(item, from: modelContext)`. Keep the `try modelContext.save()` that follows.

4. **RetentionService.swift** — In `purgeExpiredItems()` (around line 68-83): The deletion is currently in two separate loops (one for image cleanup, one for modelContext.delete). Merge into a single loop calling `deleteClipboardItemWithCleanup(item, from: modelContext)`. Keep the `try modelContext.save()` after the loop.

5. **ExpirationService.swift** — Two locations:
   - In `expireOverdueItems()` (around line 73-81): Replace `ImageStorageService.shared.deleteImage` + `modelContext.delete` with `deleteClipboardItemWithCleanup(item, from: modelContext)`. This FIXES the missing safeLabels.removeAll and URL metadata image cleanup.
   - In `performExpiration()` (around line 117-120): Replace `ImageStorageService.shared.deleteImage` + `modelContext.delete` with `deleteClipboardItemWithCleanup(item, from: modelContext)`. Note: the imagePath/thumbnailPath locals captured earlier can be removed since the helper reads them from the item directly. This also FIXES missing safeLabels.removeAll and URL metadata cleanup.

**Hash call sites (replace inline SHA256 with ContentHash):**

6. **ClipboardMonitor.swift** (around line 243-244): Replace `let digest = SHA256.hash(data: hashData)` + hex formatting with `ContentHash.hash(text: textContent)` (or `ContentHash.hash(imageData: imageData)` if hashing image data). Remove the local `import CryptoKit` if it's no longer needed.

7. **ImportExportService.swift** (around line 451-452): Replace inline SHA256 hash with `ContentHash.hash(text: ...)`. Remove `import CryptoKit` if no longer needed.

8. **ImageStorageService.swift** (around line 201-204): Replace inline SHA256 hash of image data prefix with `ContentHash.hash(imageData: data)`. Remove `import CryptoKit` if no longer needed.

After all replacements, verify no remaining direct `ImageStorageService.shared.deleteImage` calls exist that operate on ClipboardItem fields (grep to confirm). Some ImageStorageService.deleteImage calls may exist for non-item cleanup — those are fine.
  </action>
  <verify>
1. Build succeeds: `cd /Users/phulsechinmay/Desktop/Projects/pastel && xcodebuild -project Pastel.xcodeproj -scheme "Pastel Sparkle" -configuration Debug build 2>&1 | tail -5`
2. Grep confirms no remaining manual deletion patterns: `grep -rn "ImageStorageService.shared.deleteImage" Pastel/App/ Pastel/Views/ Pastel/Services/RetentionService.swift Pastel/Services/ExpirationService.swift` should return zero results.
3. Grep confirms no remaining inline SHA256: `grep -rn "SHA256.hash" Pastel/Services/ClipboardMonitor.swift Pastel/Services/ImportExportService.swift Pastel/Services/ImageStorageService.swift` should return zero results.
  </verify>
  <done>All 5 deletion call sites use deleteClipboardItemWithCleanup (with consistent safeLabels.removeAll). All 3 hash call sites use ContentHash. Build succeeds with no regressions.</done>
</task>

</tasks>

<verification>
1. `xcodebuild -project Pastel.xcodeproj -scheme "Pastel Sparkle" -configuration Debug build` succeeds
2. `grep -rn "deleteClipboardItemWithCleanup" Pastel/` shows exactly 5 call sites + 1 definition
3. `grep -rn "ContentHash.hash" Pastel/` shows exactly 3 call sites + 2 definitions (text + imageData)
4. `grep -rn "SHA256.hash" Pastel/Services/ClipboardMonitor.swift Pastel/Services/ImportExportService.swift Pastel/Services/ImageStorageService.swift` returns nothing
5. No `ImageStorageService.shared.deleteImage` calls remain in AppState, ClipboardCardView, HistoryBrowserView, RetentionService, or ExpirationService
</verification>

<success_criteria>
- deleteClipboardItemWithCleanup is the single deletion path for ClipboardItems across the entire codebase
- ContentHash enum is the single source of SHA256 hashing
- All deletions now consistently clear safeLabels (CloudKit-safe)
- All deletions now consistently clean up both clipboard images AND URL metadata images
- Build succeeds with zero warnings related to these changes
</success_criteria>

<output>
After completion, create `.planning/phases/27-service-layer-audit-refactor-combine-or-remove-redundant-services-to-reduce-app-bloat-and-improve-performance/27-01-SUMMARY.md`
</output>
